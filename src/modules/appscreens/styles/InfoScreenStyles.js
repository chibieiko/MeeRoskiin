import * as colors from '../../../res/colors.json';
import * as dimensions from '../../../res/dimensions.json';

export const InfoScreenStyles = {
    container: {
        backgroundColor: colors.backgroundColor,
        padding: dimensions.paddingVertical
    },
    text: {
        marginBottom: 24
    }
};